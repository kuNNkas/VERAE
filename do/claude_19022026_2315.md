# claude_19022026_2315

## Что сделано
Задача 7: читаемые подписи полей в форме ввода анализов.

Добавлен маппинг `FIELD_META` в `schemas.ts` — все 24 поля получили русские названия и единицы измерения (например, `LBXHGB` → "Гемоглобин (Hb), г/дл"). В форме теперь отображается читаемое название + единица + признак обязательности. Сообщения об ошибках (незаполненные поля, BMI) также переведены на читаемые имена.

## Файлы
- `frontend/src/lib/schemas.ts`
- `frontend/src/app/form/page.tsx`
