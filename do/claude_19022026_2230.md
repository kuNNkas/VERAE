# claude_19022026_2230

## Что сделано
Задачи 1-3: исправлены типы и ошибки в статусной логике.

**1. Тип AnalysisStatus** — убран лишний `"queued"`, тип приведён к реальному API (`pending | processing | completed | failed`).

**2. Дублирующиеся переменные** — убраны дублирующиеся объявления `elapsed` и `timedOut` в status-странице (строки 75-76, которые объявлялись до блока `if (error)` и затем снова после него).

**3. Коды ошибок** — в `process_analysis_job` теперь проставляются `failure_reason` и `error_message` при падении анализа: `"inference_error"` для ошибок модели, `"empty_lab_payload"` для пустых данных.

## Файлы
- `frontend/src/lib/api.ts`
- `frontend/src/app/analyses/[id]/page.tsx`
- `backend/app/services/analyses_service.py`
