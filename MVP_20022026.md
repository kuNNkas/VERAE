# VERAE — состояние MVP на 20.02.2026

---

## Что из себя представляет продукт

B2C-сервис интерпретации анализов крови — аналог BloodGPT.
Пользователь вводит показатели ОАК + биохимии → получает оценку риска дефицита железа от CatBoost-модели (обучена на NHANES 2021-2023) с объяснением и клинической рекомендацией.

Стек: **Next.js 14** (Vercel) + **FastAPI + CatBoost** (Railway, Docker).

---

## Что уже сделано

### Бэкенд (FastAPI)

| Что | Состояние |
|-----|-----------|
| `POST /auth/register`, `POST /auth/login` — JWT-авторизация | ✅ |
| `POST /analyses` — создание анализа, фоновая обработка (BackgroundTasks) | ✅ |
| `GET /analyses`, `GET /analyses/{id}`, `GET /analyses/{id}/result` | ✅ |
| `POST /v1/risk/predict` — открытый эндпоинт, прямой вызов модели | ✅ |
| CatBoost-модель `ironrisk_bi_reg_29n.cbm` — загрузка и инференс | ✅ |
| Fallback без .cbm (детерминированная формула по HGB/MCV/RDW) | ✅ |
| Нормализация единиц РФ → NHANES (HGB г/л→г/дл, MCHC, глюкоза ммоль/л→мг/дл и др.) | ✅ |
| Объяснения (SHAP / fallback), risk_tier, risk_percent, clinical_action, confidence | ✅ |
| Persistent хранение анализов в SQLite (input_payload + result_payload) | ✅ |
| Correlation ID middleware, структурированные логи | ✅ |
| Тесты: test_api_flow + test_e2e_flow по OpenAPI-контракту | ✅ |
| Dockerfile, Railway деплой | ✅ (в процессе — pip timeout фикс запушен) |

### Фронтенд (Next.js)

| Что | Состояние |
|-----|-----------|
| Лендинг `/` — маркетинговая страница с hero, benefits, CTA | ✅ |
| `/login`, `/register` — формы, JWT в sessionStorage, редирект | ✅ |
| `/form` — ввод ОАК/биохимии, 3 секции (основные/антропометрия/дополнительные), коллапс доп. | ✅ |
| `/analyses` — список анализов с датами, цветными статусами, нумерацией | ✅ |
| `/analyses/[id]` — polling статуса, прогресс-стадии, таймаут 75 с | ✅ |
| `/analyses/[id]/result` — risk_tier с цветами, risk_percent, iron_index, объяснения, бар | ✅ |
| Русские метки полей, подсказки по единицам (г/л, ммоль/л и пр.) | ✅ |
| Медицинский дисклеймер на странице результата | ✅ |
| AuthGuard — защита роутов, 401 → logout + redirect | ✅ |
| Telemetry-адаптер (form_submit, api_error, result_shown) | ✅ |
| Vercel деплой | ✅ (настроен Root Directory = `frontend`) |

---

## Что нужно доделать

### 🔴 Критично (блокирует нормальную работу)

1. **Railway деплой** — пуш с `--timeout 300` в Dockerfile отправлен, нужно убедиться что билд прошёл и сервис запущен. Проверить `GET /health`.

2. **NEXT_PUBLIC_API_URL на Vercel** — прописать публичный Railway URL (`https://xxx.up.railway.app`) в переменных окружения Vercel. Без этого фронт шлёт запросы в никуда.

3. **CORS на бэкенде** — в Railway добавить env `CORS_ALLOW_ORIGINS=https://your-app.vercel.app`. Без этого браузер заблокирует запросы к API.

4. **AUTH_TOKEN_SECRET на Railway** — задать случайный секрет (не dev-default). Бэкенд в prod режиме отказывается работать с dev-секретом.

### 🟡 Важно (не работает / некрасиво)

5. **bcrypt совместимость** — `passlib[bcrypt]==1.7.4` + `bcrypt==4.2.1` = runtime AttributeError при хэшировании паролей. В `requirements.txt` уже опущено до `bcrypt==4.0.1`, нужно проверить что регистрация работает после деплоя.

6. **Smoke-тест после деплоя** — вручную проверить полный флоу: регистрация → форма → ввод данных → результат. Сейчас нет CI-pipeline для этого.

7. **DATABASE_URL на Railway** — убедиться что задан (или SQLite создаётся в volume). При рестарте контейнера без persistent volume SQLite пропадёт. Для MVP + малой нагрузки: смонтировать `/data` в Railway Volume и указать `DATABASE_URL=sqlite:////data/verae.db`.

### 🟢 Бэклог (после запуска)

| Фича | Приоритет | Комментарий |
|------|-----------|-------------|
| Populяционные перцентили (где вы среди похожих) | Высокий | В MVP_PLAN — обязательно. Нет данных на бэке пока |
| SHAP-визуализация — горизонтальные бары на фронте | Средний | Сейчас только список текстом |
| Gauge (полукруглая шкала риска) | Средний | Сейчас линейный бар |
| OCR / загрузка PDF с анализами | Низкий | Версия 2 |
| Трекинг динамики по нескольким анализам | Низкий | Версия 2 |
| Реферальная ссылка на КДЛ-партнёра (кнопка «Сдать у партнёра») | Средний | Заглушка уже есть, нужен реальный URL |
| Telegram-бот как альтернативный интерфейс | Низкий | Версия 3 |
| Alembic миграции | Низкий | Сейчас `create_all` при старте — для MVP ок |
| Prometheus / метрики | Низкий | Логи с correlation_id есть, метрик нет |
| Напоминание о пересдаче через 3 мес (email/push) | Низкий | Версия 2 |
| Платная подписка | Низкий | Версия 3 |

---

## Деплой (текущее состояние)

```
Frontend:  Vercel  — github.com/kuNNkas/VERAE, branch: master, dir: frontend
Backend:   Railway — Docker, dir: backend, port: 8000
БД:        SQLite  — в контейнере (нужен Volume для persistence)
Модель:    ironrisk_bi_reg_29n.cbm — копируется в образ при сборке (<100 MB)
```

### Env-переменные которые нужно задать

**Vercel:**
```
NEXT_PUBLIC_API_URL=https://<your-service>.up.railway.app
```

**Railway:**
```
APP_ENV=production
AUTH_TOKEN_SECRET=<случайная строка 32+ символа>
CORS_ALLOW_ORIGINS=https://<your-app>.vercel.app
DATABASE_URL=sqlite:////data/verae.db   # если смонтирован volume
```

---

## Чеклист запуска

- [ ] Railway: билд прошёл успешно (`/health` отдаёт 200)
- [ ] Railway: Volume смонтирован под SQLite (`/data`)
- [ ] Railway: все env-переменные заданы
- [ ] Vercel: `NEXT_PUBLIC_API_URL` указывает на Railway
- [ ] Smoke-тест: регистрация → ввод ОАК → результат
- [ ] CORS: запрос с Vercel-домена не блокируется браузером

---

## Критерий успеха MVP

```
Минимум:   Пользователь регистрируется, вводит ОАК, получает результат с риском
Хорошо:    50+ человек попробовали, есть обратная связь
Отлично:   Есть КДЛ-пилот, первые SMS по рекомендациям, PPV >= 60%
```
"LBXHGB": 9.0, "LBXRBCSI": 3.9, "LBXHCT": 28.1,
    "LBXMCVSI": 72, "LBXMC": 23.1, "LBXMCHSI": 32.0,
    "LBXRDW": 19.0, "LBXPLTSI": 450, "LBXWBCSI": 5.5,
    "RIDAGEYR": 30, "BMXBMI": 22, "RIAGENDR": 2,
    "LBXSGL": 90, "LBXSCH": 160, "BP_SYS": 110, "BP_DIA": 70

 Профиль 1: Явный дефицит (Должен быть HIGH / Красный)
Типичная девушка с сильной усталостью и выпадением волос.
Hb: 95
MCV: 71тзь 
MCHC: 310 (или MCH 23)
RDW: 17.5
RBC: 4.1
Hct: 29.1
Возраст: 28, Пол: 2
WBC: 6.2, PLT: 390
🟢 Профиль 2: Абсолютно здорова (Должен быть LOW / Зеленый)
Спортсменка, ест красное мясо.
Hb: 138
MCV: 89
MCHC: 340 (или MCH 30)
RDW: 12.8
RBC: 4.6
Hct: 41.0
Возраст: 32, Пол: 2
WBC: 5.8, PLT: 245
🟡 Профиль 3: Скрытый дефицит (Должен быть WARNING или GRAY / Желтый)
Латентный дефицит. Врач скажет "норма", а модель должна увидеть проблему.
Hb: 118 (чуть-чуть снижен)
MCV: 79 (по нижнему краю)
MCHC: 325 (или MCH 26)
RDW: 15.2 (уже начал расти!)
RBC: 4.3
Hct: 34.0
Возраст: 40, Пол: 2
WBC: 7.1, PLT: 310